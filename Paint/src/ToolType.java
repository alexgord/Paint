/****************************************************************************
						All the possible kinds of shapes
						one can use. By extension, this is also
						a list of all the shapes one can draw
 ****************************************************************************/
public enum ToolType
{
	LINE, RECTANGLE, ELLIPSE, POLYLINE, POLYGON;
}
